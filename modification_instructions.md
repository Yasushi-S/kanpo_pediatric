# 修正指示書（kanpo_pediatric）

初回実装は `creation_order.md` の内容に基づく。以後の改修指示はこのファイルの末尾に追記する。

---

## [2026-08-09] 追加指示 #1 ── 初回実装の未完了部分の実装

### 背景
Claude Code によるレビューの結果、`app.py`（データ構造・スコアリング・併用安全性チェックロジック）は `creation_order.md` の内容に沿って概ね適切に実装されていることを確認した。一方で以下の要素が未実装であり、このままでは起動してもトップページでエラーになる。修正後、再度 Claude Code レビューを経てからコミットすること。

### 対応事項

1. **`templates/index.html` の作成（最優先・未作成）**
   - `app.py` の `index()` が `render_template("index.html", symptom_categories=..., life_stages=..., symptom_life_stages=...)` を呼んでいるが、`templates/` ディレクトリ自体が存在しない。
   - `creation_order.md` 3節の仕様（既存 `kanpo/templates/index.html` をベースに流用、ライフステージのセグメントコントロール、1歳未満の赤系強調警告、年齢換算後の目安量表示、症状チップのライフステージ連動フィルタ・自動選択解除トースト等）に従って作成する。

2. **`templates/errors/404.html` / `templates/errors/500.html` の作成（未作成）**
   - `app.py` のエラーハンドラが参照しているが存在しない。

3. **`static/` 配下の CSS / JS の作成（必要に応じて）**
   - index.html の表示・ライフステージ切替時の症状フィルタ処理に必要な JS/CSS を作成する。

4. **`requirements.txt` に `python-dotenv` を追加**
   - `app.py` は `from dotenv import load_dotenv` を try/except ImportError で囲んでおり、未インストール環境では `.env` からの `SECRET_KEY` 読み込みが無言で失敗する。`creation_order.md` 7節「SECRET_KEY は .env から読み込む」の要件を満たすため、バージョン固定のうえ追記する。

5. **`setup_firewall.ps1` / `setup_task_scheduler.ps1` の作成（未作成）**
   - `creation_order.md` 5節のファイル構成に記載の通り、既存アプリ（`kanpo`）から移植しポート番号を 50005 に調整する。

6. **`modification_log.md` への実施記録の追記**
   - `.cursor/rules` の運用ルール（コーディング完了後、実施内容を追記する）に従い、初回実装（`creation_order.md` 対応分）および本指示への対応内容を `modification_log.md` 末尾に記録すること。記録漏れがないようにする。

### 確認事項
- 医療内容（生薬量・年齢換算係数・危険閾値・ツムラ番号・エビデンス等）は `creation_order.md` 12節の通り医療従事者レビュー前の暫定値であり、実運用開始前に必ず内容確認を行うこと。UI上にもレビュー待ちである旨を明示すること。

---

## [2026-08-09] 追加指示 #2 ── 候補処方の拡充（8処方追加・19→27処方）

### 背景
既存の `KAMPO_DATABASE`（19処方）は `creation_order.md` 作成時点での初期案であり、症状カテゴリを埋めるための代表例に過ぎず、小児科領域で実際に用いられる処方を網羅していない。Claude Code のレビューで指摘した以下の処方が候補から漏れているため追加する。

### 対応事項

1. **`KAMPO_DATABASE` に以下8処方を追加する**（既存19処方と同一のフィールド構成・記述粒度で作成。ツムラ番号・生薬量・エビデンス・`life_stage_eligible` は既存処方と同様に暫定値とし、`references` に参照元を記載、添付文書での検証必須の注記を残すこと）:

   | 処方名 | ツムラ番号(暫定) | 主な適応 | 証 | 備考 |
   |---|---|---|---|---|
   | 麻黄湯 | 27 | 感冒（かぜ）初期・インフルエンザ様の高熱 | 実証 | **麻黄含有・慎重投与**（既存の五虎湯・小青竜湯・葛根湯・越婢加朮湯と同様の扱い） |
   | 桔梗湯 | 138 | 咽頭痛・扁桃炎（新規症状、下記2参照） | 実証〜中間証 | 甘草含有量が多め（要検証） |
   | 温清飲 | 57 | アトピー性皮膚炎 | 中間証〜虚証 | 麻黄・大黄・附子は含まない想定（要検証） |
   | 柴朴湯 | 96 | 喘息傾向・咳嗽遷延（不安・動悸を伴う場合） | 中間証 | 小柴胡湯＋半夏厚朴湯の合方 |
   | 半夏瀉心湯 | 14 | 口内炎（新規症状、下記2参照）・下痢 | 中間証 | 甘草含有 |
   | 芍薬甘草湯 | 68 | 腹痛（急な痙攣性腹痛） | 証を問わない | **甘草含有量が非常に多い（要検証）**。頓用的な使用が中心である旨を `notes` に明記し、長期連用時の偽アルドステロン症リスクを強調すること |
   | 五積散 | 63 | 感冒・冷えを伴う症状 | 虚証〜中間証 | 麻黄含有の可能性あり（要IF検証。含有する場合は既存の麻黄含有処方と同様の慎重投与注記が必要） |
   | 参蘇飲 | 66 | 感冒・胃腸虚弱を伴う感冒 | 虚証 | 麻黄は含まない想定（要検証） |

   各処方について `indications` / `evidence` / `onset` / `contraindications` / `precautions` / `combinations` / `side_effects` / `notes`（age_under_1 / age_1_2 / age_3_5 / age_6 / caution）/ `life_stage_eligible` を既存処方と同じ粒度で作成すること。**乳児期（1歳未満）対象は既存同様、使用実績が比較的知られているもののみ `["乳児期", "幼児期"]` とし、それ以外は `["幼児期"]` のみとする（デフォルトは幼児期のみ）**。特に麻黄湯・芍薬甘草湯は乳児期対象外とすることを強く推奨する。

2. **`SYMPTOM_CATEGORIES` に新規症状を2件追加**
   - 「呼吸器症状」カテゴリに `"咽頭痛・扁桃炎"` を追加（桔梗湯の適応先として必要）。
   - 「消化器症状」カテゴリに `"口内炎"` を追加（半夏瀉心湯の適応先として必要）。
   - 追加に伴い `SYMPTOM_LIFE_STAGES` にも両症状のライフステージ該当区分を追加すること（暫定案: 「咽頭痛・扁桃炎」は `["幼児期"]`、「口内炎」は `["乳児期", "幼児期"]`。要医学レビュー）。

3. **`HERB_COMPONENTS` に上記8処方の甘草・麻黄・大黄・附子含有量（1日量, g）を追加**
   - 特に麻黄湯・五積散（麻黄含有の可能性）・芍薬甘草湯・桔梗湯（甘草高含有の可能性）は `HERB_RISK_THRESHOLDS` との比較に直結するため、値の根拠をコメントで明記すること。
   - 麻黄湯を追加することで、麻黄含有処方が5種類（五虎湯・小青竜湯・葛根湯・越婢加朮湯・麻黄湯）になる。既存の併用安全性チェックロジック（`check_combination_safety()`）はそのまま流用可能だが、`COMBINATION_PATTERNS` に麻黄含有処方同士の組み合わせを誤って追加しないよう注意すること。

4. **`SYMPTOM_KAMPO_WEIGHTS` に新規処方・新規症状の対応を追加**
   - 新規症状（咽頭痛・扁桃炎、口内炎）を含め、既存フォーマット（`weight` 1-5、`evidence_level`、`recommendation_grade`）で追加する。

5. **`KAMPO_READING_KANA` に8処方のカタカナ読みを追加**

6. **`COMBINATION_PATTERNS` に新規処方を用いた併用パターンを2〜3件程度追加**（任意。既存19処方との組み合わせでも可）

7. **`modification_log.md` への実施記録の追記**（`.cursor/rules` の運用ルールに従う）

### 確認事項
- 処方数が19→27に増えるため、`creation_order.md` 4.2節・本ファイルに記載の暫定データはすべて実運用前に医療従事者による検証が必須である旨を改めて明示すること（UIの「レビュー待ち」表示は既存のままでよい）。
- 芍薬甘草湯・桔梗湯など甘草高含有の処方が加わることで、既存の他処方との2剤併用時に `HERB_RISK_THRESHOLDS`（甘草 warning 2.5g / danger 5.0g）に抵触しやすくなる可能性がある。`COMBINATION_PATTERNS` を追加する際は安全性チェックの結果（`check_combination_safety()`）を実際に確認し、危険な組み合わせを推奨パターンとして提示しないよう注意すること。
- 対応完了後、Claude Code のレビューを再度実施してからコミットする。

---

## [2026-08-09] 追加指示 #3 ── レビュー指摘の軽微な修正2点

### 背景
追加指示#2（8処方追加）の Claude Code レビューで、実害はないものの整合性上望ましくない点が2件見つかった。

### 対応事項

1. **`combinations.avoid` の非対称性を解消する**
   - 新規追加した麻黄湯・五積散の `avoid` には互いおよび既存の麻黄含有処方（五虎湯・小青竜湯・葛根湯・越婢加朮湯）が列挙されているが、既存4処方側の `avoid` リストには麻黄湯・五積散が追加されておらず片方向のみになっている。同様に芍薬甘草湯の `avoid`（甘麦大棗湯・桔梗湯・小建中湯・桂枝加芍薬湯）についても、相手側4処方の `avoid` に芍薬甘草湯が追加されているか確認し、漏れていれば追加する。
   - `combinations.avoid` は現状どのロジックからも参照されない表示専用データ（実際の安全性判定は `COMBINATION_PATTERNS` + `check_combination_safety()` の生薬量計算で行われる）である。この位置付けを誤解されないよう、`KAMPO_DATABASE` 定義の直前または `combinations` フィールドの説明部分に「`avoid` は参考情報であり、実際の併用安全性チェックは `check_combination_safety()` の生薬量計算で行う」旨のコメントを追加する。

2. **古いコメントの数値を更新する**
   - `app.py` 116行目付近「症状データベース（4カテゴリ・26症状）」→「4カテゴリ・28症状」に更新。
   - `app.py` 1866行目付近「2剤併用の推奨パターン（19処方から選定・暫定）」→ 実際の処方数（27処方）に整合する記述に更新。
   - 他にも処方数・症状数に言及する古いコメントが残っていないか `app.py` 全体を検索し、あれば併せて更新する。

3. **`modification_log.md` への実施記録の追記**

### 確認事項
- 上記は表示・ドキュメント上の整合性修正であり、スコアリング・安全性チェックのロジック自体は変更しないこと。
- 対応完了後、Claude Code のレビューを再度実施してからコミットする。

---

## [2026-08-09] 追加指示 #4 ── 複数メーカー対応（ツムラ／クラシエ／コタロー）

### 背景
現状は全処方がツムラ製剤前提（`tsumura_no`、`HERB_COMPONENTS` の値も単一）で実装されている。実際にはメーカーによりエキス化率・配合比率の違いから生薬量（g/日）が異なる場合があり、本アプリの併用安全性チェック（`check_combination_safety()`）は生薬量の数値計算に依存しているため、メーカー差を無視すると誤った安全性判定になりうる。ユーザー（発注者）承認のうえ、対応メーカーを **ツムラ／クラシエ／コタロー（小太郎漢方製薬）** の3社に拡張する。

データ精度の方針: 27処方 × 3メーカー = 81通りをすべて暫定値で一気に登録する（段階導入はしない）。ただし全て一般的な漢方知識に基づく暫定値であり、**実運用前に薬剤師が各メーカーの添付文書・インタビューフォームで必ず検証すること**を、既存の19→27処方拡充時よりも一段階強くUI・コード双方で明示する（メーカー差は最も誤りが起きやすい箇所であるため）。

### 対応事項

1. **`MANUFACTURERS` 定数を新設する**（`LIFE_STAGES` と同様、既存ロジックへの侵襲を最小化する追加軸として実装）
   ```python
   MANUFACTURERS = {
       "ツムラ": {"label": "ツムラ", "order": 1},
       "クラシエ": {"label": "クラシエ", "order": 2},
       "コタロー": {"label": "コタロー（小太郎漢方製薬）", "order": 3},
   }
   DEFAULT_MANUFACTURER = "ツムラ"
   ```

2. **`KAMPO_DATABASE` の `tsumura_no`（単一フィールド）を `products`（メーカー別）に置き換える**
   ```python
   "products": {
       "ツムラ": {"product_no": 99, "available": True},
       "クラシエ": {"product_no": None, "available": False},  # 例: 取り扱いなしの場合
       "コタロー": {"product_no": None, "available": True},
   },
   ```
   - 27処方すべてについて3メーカー分を作成する。各メーカーが実際にその処方を製造しているか（`available`）は一般的に知られている範囲で暫定判断してよいが、不明な場合は `available: True` とし、コメントで「取扱有無要確認」と明記する。
   - `available: False` の処方は、そのメーカー選択時に推奨候補から除外する（`life_stage_eligible` と同様の絞り込みロジックを追加）。

3. **`HERB_COMPONENTS` をメーカー別のネスト構造に変更する**
   ```python
   HERB_COMPONENTS = {
       "小建中湯": {
           "ツムラ": {"甘草": 2.0, "麻黄": 0, "大黄": 0, "附子": 0},
           "クラシエ": {"甘草": 2.0, "麻黄": 0, "大黄": 0, "附子": 0},  # 暫定: ツムラと同値と仮定・要IF検証
           "コタロー": {"甘草": 2.0, "麻黄": 0, "大黄": 0, "附子": 0},  # 暫定: 同上
       },
       ...
   }
   ```
   - メーカー間で値が異なることが一般的に知られている処方（例: 甘草・麻黄含有量が製剤によって差が出やすいもの）があれば、その根拠をコメントに明記する。不明な場合は「ツムラと同値と仮定・要IF検証」等、仮定であることを明示すること（実態を知らないのに値を変えて捏造しないこと）。

4. **`check_combination_safety()` にメーカー引数を追加する**
   - シグネチャを `check_combination_safety(kampo_list, age_key=None, manufacturer=DEFAULT_MANUFACTURER)` に変更し、`HERB_COMPONENTS[kampo][manufacturer]` を参照するようにする。指定メーカーにその処方のデータが存在しない場合はエラーではなく安全側（ツムラ値を代用しつつ `"data_fallback": True` 等のフラグを結果に含める）で処理する。

5. **`/recommend` エンドポイント・`calculate_kampo_scores()` 等にメーカー絞り込みを追加**
   - リクエストボディに `manufacturer` を追加（未指定時は `DEFAULT_MANUFACTURER`）。
   - `products[manufacturer]["available"]` が `False` の処方はライフステージ絞り込みと同様に候補から除外する。
   - レスポンスの各処方情報に、選択メーカーの `product_no` を含める（既存の `tsumura_no` 直接参照箇所は全て `products[manufacturer]` 経由に置き換える）。

6. **UI（`templates/index.html`）にメーカー選択を追加する**
   - ライフステージと同様、入力パネル最上部付近にメーカー選択（セグメントコントロールまたはプルダウン、デフォルト=ツムラ）を追加する。必須項目とする。
   - 推奨カード・併用提案カードの「ツムラXX番」表示を、選択中メーカーの製品番号表示に置き換える。
   - メーカー切替時、選択中の処方候補が変わりうる旨を軽く案内する（ライフステージ切替時のトースト通知と同様の扱いでよい）。

7. **`modification_log.md` への実施記録の追記**

### 確認事項
- **本追加は特に医療安全性に直結する。** メーカー別の生薬量データはすべて未検証の暫定値であり、実運用開始前に薬剤師が各メーカー公式の添付文書・インタビューフォームで全件照合するまでは、複数メーカー選択機能自体を実務では使用しないよう、画面上に強い注意書き（例: 「メーカー別データは未検証です。差し替え前に必ず添付文書で確認してください」）を追加すること。
- 既存のツムラ単体データ（19→27処方拡充分含む）は今回変更しないこと（`products.ツムラ.product_no` に既存の `tsumura_no` 値をそのまま移設する）。
- 対応完了後、Claude Code のレビューを再度実施してからコミットする。
