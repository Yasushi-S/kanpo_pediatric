# 作成発注書 ── 小児科漢方薬推奨システム (kanpo_pediatric)

作成日: 2026-08-08
発注者: 院内利用者（医療従事者）
参照元: 既存アプリ `kanpo`（婦人科漢方薬推奨システム）と同一ロジック・同一アーキテクチャを流用し、小児科向けに新規構築する。

---

## 1. アプリ概要・目的

- 小児科領域の症状・体質（証）に基づいて漢方薬候補を推奨する院内向け Web アプリ。
- 既存の婦人科版アプリ（`C:\Users\shita\github\kanpo`）とスコアリングロジック・安全性チェックロジックを共通化し、データ（症状カテゴリ・漢方薬データベース・重み付け）のみを小児科向けに差し替える。
- **対象ユーザー**: 医療従事者（医師・薬剤師）。最終的な処方判断は医師が行う参考ツールであり、診断・処方を確定するものではない。
- **対象年齢層**: 乳幼児（0〜6歳）。学童期以降（7歳以上）は対象外とし、UI上にも明記する。
  - 特に1歳未満は体格差・発達差が大きく漢方薬の使用経験も限定的なため、UI上で「1歳未満は医師の個別判断が必須。参考情報としてのみ使用可」と強調表示する。

---

## 2. 機能一覧

既存 `kanpo` アプリの機能をそのまま踏襲する：

1. 症状の複数選択（カテゴリ別チップ表示、カテゴリは折りたたみ可・先頭カテゴリのみ初期展開）。**表示される症状は選択中のライフステージ（下記3）に応じてフィルタする。**
2. 証（虚証・中間証・実証・指定なし）の選択
3. **ライフステージ・年齢入力（新規・小児科版のみの追加要件。婦人科版の「月経関連／更年期関連」カテゴリ分けに相当する軸として、年齢層でアプリの表示・推奨候補を分ける）**:
   - まず「乳児期（0歳〜1歳未満）」「幼児期（1歳〜6歳）」の2択をセグメントコントロール等で選択する。
   - 「乳児期」選択時は年齢細分は「1歳未満」の1区分のみで、常に画面上部に赤系の強調警告（医師の個別判断が必須）を表示する。
   - 「幼児期」選択時は続けて「1〜2歳」「3〜5歳」「6歳」のいずれかを選択する。
   - ライフステージの選択により、(a) 表示される症状チップ、(b) 推奨候補となる漢方薬の母集団、(c) 用量換算係数、の3つが切り替わる。データ定義は `LIFE_STAGES` として定数化する（詳細は 4.1a・4.2・4.4 節）。
4. エビデンスを考慮したスコアリングで上位3剤を表示（エビデンスレベル・ガイドライン推奨度バッジ、証の適合度、発現時期の目安）
5. 選択症状からの証（体質）推定（参考情報、僅差時は非断定）
6. 2剤併用案の提示 + 生薬量（甘草・麻黄・大黄・附子）に基づく併用安全性チェック
   - **小児科版の追加要件**: 生薬量は成人量（添付文書の1日量）をベースに保持しつつ、選択された年齢細分に対応する用量換算係数（1歳未満=1/4以下、1〜2歳=1/3、3〜5歳=1/2、6歳=1/2）を乗じたうえで閾値判定を行う。
   - 推奨カード上に「年齢換算後の目安量」を表示する。
7. 印刷 / PDF 保存（ブラウザの印刷機能、既存 CSS の @media print を流用）
8. 条件を保持した再計算（症状・証・年齢の選択を保ったまま結果エリアのみ更新）
9. 入力不足時のインラインエラー表示
10. 推奨結果・併用提案に漢方薬名の読み仮名（カタカナ）を併記
11. 操作ログ記録（`instance/operation_log.db`、SQLite、パラメータバインド必須）

---

## 3. 画面・UI仕様

- 既存 `kanpo/templates/index.html` の構成・デザイン（レイアウト、幅900px超で2カラム表示、症状チップ、結果カードのスタイル）をベースに流用する。文言・配色トークンはそのまま踏襲してよい。
- ヘッダー・タイトルを「小児科漢方薬推奨システム」に変更する。
- 免責文言（「本システムは参考用です。最終的な処方判断は医師が行ってください。」）は必ず残す。加えて「対象年齢: 乳幼児（0〜6歳）。7歳以上には使用しないでください。」を明記する。
- 症状選択エリアの最上部に「ライフステージ」セグメントコントロール（乳児期 / 幼児期）を必須項目として配置する。婦人科版の「月経関連」「更年期関連」のようなカテゴリ分けに相当する、本アプリの一番大きな分岐軸として扱う。
  - 「乳児期」選択時: 年齢細分の選択肢は「1歳未満」のみ表示（実質固定）。結果表示エリア上部に赤系の強調ボックスで「1歳未満は医師の個別判断が必須です。参考情報としてのみご使用ください。」を常時表示する。
  - 「幼児期」選択時: 年齢細分セレクト（1〜2歳 / 3〜5歳 / 6歳）を表示する。
  - ライフステージを切り替えると、選択済みの症状チップのうち新しいステージで非該当のものは自動的に選択解除し、その旨を軽くトースト等で通知する。
- ファビコンは既存アプリと区別できるものを使用する（既存: 🌿）。小児科版は 👶 を仮案とし、発注者の確認を得ること。

---

## 4. データ構造

`app.py` 内に既存アプリと同一のスキーマで定義する（DBファイルではなく Python の辞書・リスト定数として保持する点も既存と同様）。

### 4.1 SYMPTOM_CATEGORIES（4カテゴリ・計26症状・乳幼児向けに再設計）

```python
SYMPTOM_CATEGORIES = {
    "消化器症状": [
        "腹痛", "便秘", "下痢", "嘔吐", "食欲不振", "乳児疝痛（コリック）", "便秘・下痢を繰り返す",
    ],
    "呼吸器症状": [
        "咳（乾性）", "咳（湿性・痰がらみ）", "喘息傾向", "鼻水・鼻づまり",
        "感冒（かぜ）初期", "感冒の遷延・長引く微熱", "中耳炎を繰り返す",
    ],
    "夜泣き・かんしゃく・神経症状": [
        "夜泣き", "かんしゃく・疳の虫", "不眠", "情緒不安定", "熱性けいれん後の体調不良",
    ],
    "虚弱体質・皮膚・その他": [
        "虚弱体質・疲れやすい", "食が細い", "体重増加不良", "寝汗（盗汗）",
        "アトピー性皮膚炎", "湿疹・おむつかぶれ", "病後の体力低下",
    ],
}
```

### 4.1a LIFE_STAGES / SYMPTOM_LIFE_STAGES（ライフステージによる分割・新規要件）

婦人科版の「月経関連」「更年期関連」カテゴリ分けに相当する、本アプリ最上位の分岐軸として「ライフステージ」を導入する。`SYMPTOM_CATEGORIES` の構造・既存のカテゴリ表示ロジックは変更せず、別途ステージ判定用の定数を追加してテンプレート/JS側で表示フィルタをかける方式とする（既存レンダリングロジックへの侵襲を最小化するため）。

```python
LIFE_STAGES = {
    "乳児期": {
        "label": "乳児期（0歳〜1歳未満）",
        "age_options": [
            {"key": "under_1", "label": "1歳未満", "dose_factor": 0.25},
        ],
        "warning": "乳児期は漢方薬の使用実績・エビデンスが特に限定的です。医師の個別判断を必須としてください。",
    },
    "幼児期": {
        "label": "幼児期（1歳〜6歳）",
        "age_options": [
            {"key": "1-2", "label": "1〜2歳", "dose_factor": 0.333},
            {"key": "3-5", "label": "3〜5歳", "dose_factor": 0.5},
            {"key": "6", "label": "6歳", "dose_factor": 0.5},
        ],
        "warning": None,
    },
}

# 各症状が該当するライフステージ（"乳児期", "幼児期", または両方）
SYMPTOM_LIFE_STAGES = {
    "腹痛": ["幼児期"],
    "便秘": ["乳児期", "幼児期"],
    "下痢": ["乳児期", "幼児期"],
    "嘔吐": ["乳児期", "幼児期"],
    "食欲不振": ["幼児期"],
    "乳児疝痛（コリック）": ["乳児期"],
    "便秘・下痢を繰り返す": ["乳児期", "幼児期"],
    "咳（乾性）": ["乳児期", "幼児期"],
    "咳（湿性・痰がらみ）": ["乳児期", "幼児期"],
    "喘息傾向": ["幼児期"],
    "鼻水・鼻づまり": ["乳児期", "幼児期"],
    "感冒（かぜ）初期": ["乳児期", "幼児期"],
    "感冒の遷延・長引く微熱": ["乳児期", "幼児期"],
    "中耳炎を繰り返す": ["乳児期", "幼児期"],
    "夜泣き": ["乳児期", "幼児期"],
    "かんしゃく・疳の虫": ["幼児期"],
    "不眠": ["乳児期", "幼児期"],
    "情緒不安定": ["幼児期"],
    "熱性けいれん後の体調不良": ["幼児期"],
    "虚弱体質・疲れやすい": ["幼児期"],
    "食が細い": ["幼児期"],
    "体重増加不良": ["乳児期", "幼児期"],
    "寝汗（盗汗）": ["幼児期"],
    "アトピー性皮膚炎": ["乳児期", "幼児期"],
    "湿疹・おむつかぶれ": ["乳児期", "幼児期"],
    "病後の体力低下": ["幼児期"],
}
```

上記の対応は初期案であり、Cursor 実装時・レビュー時に医学的妥当性を確認すること（特に「乳児疝痛」「情緒不安定」「かんしゃく・疳の虫」の年齢区分は臨床的に幅があるため要確認）。

### 4.2 KAMPO_DATABASE（初期案・19処方）

既存アプリと同一のフィールド構成（`tsumura_no`, `insurance`, `sho`, `description`, `indications{primary/secondary/off_label}`, `evidence{level/guideline_grade/references/efficacy_rate/clinical_usage_rank}`, `onset{initial/optimal/type}`, `contraindications`, `precautions`, `combinations{recommended/possible/avoid}`, `side_effects{common/rare/serious/note}`, `notes{...}`）を踏襲する。

`notes` は成人向け（pregnancy/lactation/elderly/pediatric）ではなく、小児科版として以下に差し替える:

```python
"notes": {
    "age_under_1": "1歳未満の使用可否・留意点（医師相談必須の旨を必ず記載）",
    "age_1_2": "1〜2歳の用量目安・留意点",
    "age_3_5": "3〜5歳の用量目安・留意点",
    "age_6": "6歳の用量目安・留意点",
    "caution": "苦味が強く乳幼児が服用を嫌がる場合の対処（服薬ゼリー・お湯で溶いて冷ます等）の要否",
}
```

さらに各処方に `"life_stage_eligible": ["乳児期", "幼児期"]`（または `["幼児期"]` のみ）を追加する。**乳児期（1歳未満）は使用実績・エビデンスが限定的な処方が多いため、デフォルトは `["幼児期"]` のみとし、乳児期での使用実績が比較的知られている処方のみ `["乳児期", "幼児期"]` とする。** ライフステージが「乳児期」のとき、推奨エンジンは `life_stage_eligible` に "乳児期" を含まない処方を候補から除外する。

対象19処方（ツムラ番号は暫定値。**実際の添付文書で必ず検証すること**）。麻黄含有処方（五虎湯・小青竜湯・葛根湯・越婢加朮湯）は乳幼児では特に慎重投与とし、`precautions` に明記する:

| 処方名 | ツムラ番号(暫定) | 主な適応カテゴリ | 証 | 乳児期対象(暫定・要検証) |
|---|---|---|---|---|
| 小建中湯 | 99 | 消化器／虚弱体質 | 虚証 | ○ |
| 黄耆建中湯 | 98 | 虚弱体質 | 虚証 | × |
| 六君子湯 | 43 | 消化器 | 虚証 | ○ |
| 人参湯 | 32 | 消化器 | 虚証 | × |
| 桂枝加芍薬湯 | 60 | 消化器 | 虚証 | × |
| 五苓散 | 17 | 消化器 | 証を問わない | ○ |
| 柴胡桂枝湯 | 10 | 消化器／呼吸器 | 中間証 | ○ |
| 麦門冬湯 | 29 | 呼吸器 | 虚証〜中間証 | × |
| 五虎湯 | 95 | 呼吸器（麻黄含有・慎重投与） | 中間証〜実証 | × |
| 小青竜湯 | 19 | 呼吸器（麻黄含有・慎重投与） | 虚証〜中間証 | × |
| 葛根湯 | 1 | 呼吸器（麻黄含有・慎重投与） | 実証 | × |
| 小柴胡湯 | 9 | 呼吸器 | 中間証 | ○ |
| 抑肝散 | 54 | 夜泣き・かんしゃく・神経症状 | 虚証〜中間証 | ○ |
| 抑肝散加陳皮半夏 | 83 | 夜泣き・かんしゃく・神経症状 | 虚証 | ○ |
| 甘麦大棗湯 | 72 | 夜泣き・かんしゃく・神経症状 | 虚証 | ○ |
| 補中益気湯 | 41 | 虚弱体質 | 虚証 | × |
| 十全大補湯 | 48 | 虚弱体質 | 虚証 | × |
| 消風散 | 22 | 虚弱体質・皮膚症状 | 中間証〜実証 | × |
| 越婢加朮湯 | 28 | 虚弱体質・皮膚症状（麻黄含有・慎重投与） | 実証 | × |

上記「乳児期対象」列はごく暫定的な仮案であり（8処方のみ○）、Cursor 実装時・レビュー時に医療従事者が添付文書等で必ず検証すること。結果として乳児期選択時は候補数が少なくなる場合があるが、これは既存アプリのステータスチェック（推奨0件時メッセージ機能）で対応可能である。

各処方の `indications` / `evidence` / `contraindications` / `side_effects` は、既存 `kanpo/app.py` の `KAMPO_DATABASE` エントリの記述粒度・文体に合わせて Cursor が作成する。エビデンスソースは以下を優先的に参照する:

- ツムラ医療用漢方製剤 添付文書・インタビューフォーム
- 日本東洋医学会 EBM 漢方（小児科領域）
- 日本小児心身医学会 関連ガイドライン
- 厚生労働省 保険適用医薬品リスト

### 4.3 SYMPTOM_KAMPO_WEIGHTS

既存アプリと同一形式（症状名 → {処方名: {"weight": int(1-5), "evidence_level": "A/B/C", "recommendation_grade": "1A/1B/2A/2B/2C"}}）で、上記19処方と26症状の対応表を作成する。1症状あたり2〜5処方程度を目安とする。

### 4.4 HERB_COMPONENTS / HERB_RISK_THRESHOLDS / AGE_DOSE_FACTORS（併用安全性チェック）

既存アプリの仕組み（甘草・麻黄・大黄・附子の1日量合計を算出し、`HERB_RISK_THRESHOLDS` の warning/danger と比較）をそのまま流用する。**小児科版（0〜6歳）では以下を追加**:

```python
AGE_DOSE_FACTORS = {
    "under_1": 0.25,  # 1歳未満: 成人量の1/4以下を目安（医師相談必須）
    "1-2": 0.333,      # 1〜2歳: 成人量の1/3
    "3-5": 0.5,        # 3〜5歳: 成人量の1/2
    "6": 0.5,          # 6歳: 成人量の1/2（7歳以降で2/3に移行するのが一般的だが本アプリは6歳までを対象）
}
```

この値は 4.1a 節の `LIFE_STAGES[...]["age_options"][...]["dose_factor"]` と同一の値であり、`check_combination_safety()` 等での参照を簡潔にするための平坦化した辞書として定義する（二重管理を避けるため、実装上は `LIFE_STAGES` から動的に導出してもよい）。

`check_combination_safety()` は入力された年齢区分に応じて `HERB_COMPONENTS` の値に `AGE_DOSE_FACTORS` を乗じたうえで `HERB_RISK_THRESHOLDS`（既存アプリと同じ成人量ベースの閾値をそのまま使用）と比較する。「1歳未満」選択時は、安全判定の結果によらず常に「医師の個別判断が必須」の警告を結果に付加すること。

`HERB_COMPONENTS` の各処方の甘草・麻黄・大黄・附子含有量（1日量, g）は既存アプリの値の記法に倣い、添付文書ベースで Cursor が設定する。特に以下は含有量が多く要注意:

- 甘麦大棗湯: 甘草 5.0g（単剤でも高含有）
- 五虎湯・小青竜湯・葛根湯・越婢加朮湯: 麻黄含有処方同士の併用は特に注意。乳幼児は成人よりも麻黄の交感神経刺激作用（動悸・不眠・発汗）に敏感なため、`HERB_RISK_THRESHOLDS` の麻黄閾値をそのまま使うのではなく、乳幼児向けにより保守的な値に見直すことを検討し、根拠をコメントで明記する。

**注意事項（必ずユーザー確認を得ること）**: 生薬量・年齢換算係数・危険閾値は臨床安全性に直結するため、Cursor 実装後、必ず薬剤師・医師による内容確認を経てから運用を開始すること。本発注書の数値は初期案であり、そのまま鵜呑みにしないこと。

### 4.5 COMBINATION_PATTERNS

既存アプリと同一形式で、上記19処方から想定される2剤併用パターンを5〜10件程度、Cursor が作成する（例: 「小建中湯 + 抑肝散」＝虚弱体質でかんしゃく・夜泣きを伴う、「柴胡桂枝湯 + 六君子湯」＝反復性の体調不良と食欲不振の併存、等）。

### 4.6 KAMPO_READING_KANA

上記19処方名のカタカナ読みを既存アプリと同形式の辞書で用意する。

### 4.7 EVIDENCE_LEVEL_MULTIPLIERS / GUIDELINE_GRADE_MULTIPLIERS / SHO_MATCH_MULTIPLIERS

既存アプリの値をそのまま流用する（ロジックの共通化のため変更不要）。

### 4.8 operation_log（SQLite）

既存アプリと同一スキーマ（`id`, `operated_at`, `ip_address`, `operation`, `detail`）。パラメータバインドを使用し、文字列結合による SQL 構築は禁止（既存 `log_operation()` をそのまま移植）。

---

## 5. ファイル構成

```
kanpo_pediatric/
├── app.py                       # Flask アプリ本体（既存 kanpo/app.py の構造を踏襲）
├── requirements.txt              # 既存アプリと同一バージョン固定（Flask 3.1.2 等）
├── start_app.bat                 # 起動バッチ
├── setup_firewall.ps1            # 院内LAN公開用（既存アプリからコピーし調整）
├── setup_task_scheduler.ps1      # タスクスケジューラ登録用（既存アプリからコピーし調整）
├── .gitignore
├── .cursor/
│   └── rules
├── templates/
│   ├── index.html                # 既存 kanpo/templates/index.html を移植・文言差し替え
│   └── errors/                   # 既存 kanpo/templates/errors/ を移植
├── static/
├── instance/                      # 実行時生成（Git 対象外）
├── docs/
├── creation_order.md              # 本発注書
├── modification_instructions.md   # 以後の改修指示（今後作成）
└── modification_log.md            # 実行記録（今後作成）
```

---

## 6. セットアップ手順

```powershell
cd C:\Users\shita\github\kanpo_pediatric
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
python app.py
```

- `app.py` の待受: `app.run(host='0.0.0.0', port=50005, debug=False)`
  - 既存アプリ（port 50002）と同一PC上で同時稼働させるため、**50005** を使用する（発注者確認済み。他プロセスと衝突しないことを起動時に確認すること）。
- ブラウザ確認: `http://localhost:50005`（同一PC）、`http://<サーバーPCのIPv4>:50005`（院内LAN他端末）

---

## 7. 必須ファイルの作成指示

- `.gitignore`: 既存アプリと同内容（`venv/`, `__pycache__/`, `*.pyc`, `.env`, `*.db`, `instance/`, `.DS_Store` を最低限含める）
- `start_app.bat`:
  ```bat
  @echo off
  cd /d "%~dp0"
  call "%~dp0venv\Scripts\activate.bat"
  python "%~dp0app.py"
  ```
- `SECRET_KEY` は `.env` から読み込む（コードへのハードコード禁止）。`.env.example` を作成し、実際の `.env` はコミットしない。
- `app.run(debug=True)` は開発時のみ。納品時は `debug=False` に固定する。

---

## 8. .cursor/rules の内容

```
# Cursor ルール（kanpo_pediatric）

## 基本方針
- 作成発注書（creation_order.md）および modification_instructions.md の指示に従い、忠実にコーディングを行う
- DB スキーマ・既存データへの変更を伴う場合は、必ず作業前にユーザーに確認を取る
- コーディング完了後、modification_log.md の末尾に実施内容を追記する（新規ファイル作成禁止）
- 修正対象ファイルを必ず読んでから変更する（読まずに変更禁止）
- 発注書・修正指示書に記載のない変更（リファクタリング・コメント追加等）は行わない
- SQL クエリはパラメータバインディングを使用（f-string での SQL 組み立て禁止）
- シークレットキー・API キーをコードにハードコードしない（必ず .env から読み込む）
- 生薬量・年齢換算係数・危険閾値など臨床安全性に関わる数値を変更する場合は、必ずその根拠をコメントで明記し、ユーザー（医療従事者）に確認を取る
- Claude Code のレビューが完了するまで git commit しない

## 実行記録フォーマット（modification_log.md）
---
## [YYYY-MM-DD] 実行 #N ── <一行要約>

### 対応した指示
- （creation_order.md または modification_instructions.md の該当箇所）

### 実施内容詳細
（実際に行った変更を箇条書きで記載）

### 確認事項・備考
（問題があれば記載、なければ「なし」）
---

## メインファイル
- app.py（Flask アプリ本体）

## ドキュメント
- creation_order.md（作成発注書）
- modification_instructions.md（修正指示）
- modification_log.md（実行記録）
```

---

## 9. GitHub リポジトリ

- 新規リポジトリを作成する（発注者確認済み）。リポジトリ名は `kanpo_pediatric` を推奨（発注者最終確認要）。
- `git init` → `.gitignore` 作成 → 初回コミットまでを Cursor 実装完了後、Claude Code レビュー通過後にユーザー承認のうえで実施する（CLAUDE.md フェーズ6準拠、ユーザー承認なしの push 禁止）。

---

## 10. タスクスケジューラ登録手順（参考）

- トリガー: コンピューターの起動時
- 操作: `kanpo_pediatric\start_app.bat` を実行
- 全般設定: 最上位の特権で実行にチェック、ユーザーがログオンしていなくても実行
- 既存アプリのタスクとは別名（例: `kanpo-pediatric-app`）で登録すること

---

## 11. セキュリティ要件（院内ネットワーク限定アプリ）

CLAUDE.md の「院内ネットワーク限定アプリ」要件に準拠する:

- `SECRET_KEY` は `.env` で管理し、コードにハードコードしない
- `app.run(debug=True)` は開発時のみ。本番では `debug=False`
- SQL はパラメータバインドを使用し、文字列結合による SQL 構築を禁止
- テンプレートでは `{{ }}` による自動エスケープを維持し、`| safe` フィルタを安易に使用しない
- アクセス元 IP をローカルネットワークに制限することを検討する
- 操作ログ（誰がいつ何をしたか）を記録する（`operation_log.db`、既存アプリと同様に実装）
- `start_app.bat` でバインドアドレスを `0.0.0.0` にする場合は、ファイアウォールで外部アクセスを遮断する（`setup_firewall.ps1` を既存アプリから移植・ポート番号のみ50005に調整）

---

## 12. 注意事項（デグレードリスク・Cursor が確認すべき事項）

1. **既存アプリ（`kanpo`）のファイルには一切手を加えないこと。** 完全に独立した新規ディレクトリ・新規Git管理とする。
2. 本発注書に記載の漢方薬データ（適応・エビデンス・生薬量・危険閾値・年齢換算係数）は初期案であり、**医療従事者（発注者）による内容確認・修正を経てから実運用を開始すること**。Cursor 実装完了時点では「レビュー待ち・医療内容未検証」の状態であることをユーザーに明示すること。
3. 年齢入力機能・年齢換算係数の適用は既存アプリに存在しない小児科版固有の追加要件であるため、既存 `check_combination_safety()` のロジックをそのまま流用せず、年齢係数適用箇所を明確にコメントで示すこと。
4. **対象が0〜6歳の乳幼児であることを踏まえ、1歳未満選択時は必ず強調警告を表示すること。** また麻黄含有処方（五虎湯・小青竜湯・葛根湯・越婢加朮湯）を推奨結果に含める際は、乳幼児での慎重投与について `precautions` に明記すること。
5. ポート番号（50005）が実際の運用PCで空いているか、起動前に再度確認すること。
6. ツムラ番号・生薬含有量・適応症・エビデンスレベルは記載内容を鵜呑みにせず、添付文書・インタビューフォーム等の一次情報で必ず検証すること。特に乳幼児への適応・用量は添付文書上「小児等への投与」の記載が処方により差があるため、処方ごとに個別確認すること。
7. **ライフステージ（乳児期／幼児期）による症状・処方候補のフィルタリングは本アプリ固有の新規要件であり、既存アプリのスコアリング・推奨処理を呼び出す前段の絞り込みとして実装すること。** `SYMPTOM_LIFE_STAGES` および各処方の `life_stage_eligible` の初期値（特に「乳児期対象」が○の8処方）は暫定の仮案であり、医療従事者によるレビューで見直される前提であることをユーザーに明示すること。

---

以上の内容でよろしければ承認をお願いします。承認後、Cursor に本発注書を渡して実装を進めます。
